apiVersion: v1
kind: Service
metadata:
  name: zipkin
  namespace: monitoring-tools
  labels:
    name: grafana
spec:
  selector:
    app: zipkin
  ports:
    - name: zipkin
      port: 80
      targetPort: 3000
  type: LoadBalancer
